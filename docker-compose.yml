services:
  nebula:
    build:
      context: .
      args:
        HERMES_GIT_REF: main
    env_file: .env
    environment:
      PORT: "3000"
    ports:
      - "3000:3000"
    volumes:
      - nebula-data:/data
    restart: unless-stopped

volumes:
  nebula-data:
